#!/usr/bin/env python
"""Server-scale ladder run on the RTX Pro 6000 (Molab).

The v5 laptop run distilled a 128x2 net from 62k teacher decisions
(96.4% teacher-decision accuracy, L1 optimum, L2 100%/2.44) and DAgger
moved L2 to 2.51 at 99% — then stalled at the level-2 gate: the policy's
97% win rate vs the beam's 100% blocked advancement, and the remaining
gap was measured as scale (data rounds, net width).

This run is the scale-up: a fresh 256x3 net (202k params), 8k teacher
episodes per level, DAgger rounds inside the ladder, 300-episode gates,
19 forkserver workers. Same code as the laptop (GPU-first design paying
off: --device is the only machine-specific knob).

Usage (on the server):
    python3 examples/cheese_learner.py \
        --hidden 256 --layers 3 \
        --distill-episodes 8000 --distill-epochs 60 --distill-chunk 20000 \
        --dagger-rounds 3 --dagger-episodes 400 --dagger-epochs 2 \
        --iterations 8 --episodes 96 \
        --gate-episodes 300 \
        --max-level 10
"""
