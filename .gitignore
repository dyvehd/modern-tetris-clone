# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# session artifacts
logs/
tmp/
tetris_*.png

# personal settings (keybinds/handling); defaults live in config.py
settings.toml

# local notes
.local_note.txt

# secrets — never commit credentials
.env
.env.*

# built bot backends (bots/build_bots.sh regenerates them)
bots/build/
bots/fusion-shim/target/
bots/blockfish-shim/target/
tmp
